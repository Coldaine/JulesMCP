# Jules Control Room backend configuration
# Copy this file to backend/.env and replace the placeholder values.

# Required: token shared with trusted clients on the local network.
LOCAL_TOKEN=change-me

# Required for Jules API access.
JULES_API_KEY=replace-with-real-key

# Optional: override the listening port (defaults to 3001)
# PORT=3001

# Optional: comma-separated IPv4 prefixes permitted to reach the API when
# exposing on a LAN (e.g. "192.168.1.,10.0.0."). Leave blank to allow only
# localhost/loopback addresses.
# ALLOWLIST=

# Optional: enable persistence of session snapshots to sql.js on disk.
# PERSIST=0

# Optional: specify explicit allowed origins when serving a remote frontend.
# CORS_ORIGIN=

# Optional: webhook invoked with session deltas (POST JSON).
# NOTIFY_WEBHOOK=
